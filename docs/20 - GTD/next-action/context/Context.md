# Context

A context is an execution constraint or grouping that helps decide which next action is available now.

Examples include places, tools, people, or working modes. The app supports context filtering so the next action list can show only actions relevant to the current situation.

The context as an element describes one execution constraint. The page that renders contexts is [[Contexts]].

Related pages: [[Next Actions]], [[Contexts]].
