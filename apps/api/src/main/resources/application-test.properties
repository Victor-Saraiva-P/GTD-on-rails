
# WHY: Spring caches multiple test ApplicationContexts. A unique in-memory database per
# context prevents cached Hikari pools from sharing tables and locks through SQLite shared-cache.
spring.datasource.url=jdbc:sqlite:file:testdb-${random.uuid}?mode=memory&cache=shared
spring.datasource.driver-class-name=org.sqlite.JDBC
# WHY: SQLite serializes writes; one test connection removes self-contention inside a context.
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.show-sql=true
spring.web.resources.add-mappings=false
spring.flyway.locations=classpath:db/sqlite-migration
spring.flyway.placeholders.databaseIdentity=TEST
spring.flyway.placeholders.databaseEnvironment=TEST
gtd.database.environment=TEST
api.project-name=https://gtdonrails.local
api.base-url=/errors
gtd.data.root-directory=./build/data
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://127.0.0.1:1420,http://localhost:1420}
gtd.assets.local-directory=./build/data
gtd.cleanup.enabled=false
gtd.cleanup.retention-days=30
gtd.google.token-encryption-key=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
# WHY: External sync is disabled in tests unless a test explicitly enables a fake server.
gtd.sync.server.enabled=false
