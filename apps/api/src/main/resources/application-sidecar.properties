server.address=127.0.0.1
server.port=0
# Native EventSource runs inside the Tauri WebView, unlike plugin-http requests.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:tauri://localhost,http://tauri.localhost,https://tauri.localhost}
gtd.sidecar.ready-file=${GTD_SIDECAR_READY_FILE:}
